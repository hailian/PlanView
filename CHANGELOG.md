# PlanView 版本说明

按 git tag 记录各版本变更。最新版本见文末。

---

## v0.1 — 初始版本：帧数据源与协议组态基础（2026-09-13）

- 重构项目为模块化多目标架构（`LogicPlanner` / `PageViewer` / `pv_base` 静态库），核心设计：组件绘制代码只存在于 `ComponentRenderer`，设计器与运行器共用，由链接期保证所见即所得
- 添加 DPI/HiDPI 缩放支持与现代深色主题；调色板按钮尺寸/列数随 DPI 与文本自适应
- 新增**帧数据源组件**（TCP/UDP + 拆帧 + 自配置规约）
- 拆分**数据源 / 协议配置**组件并按名称关联，补充关联校验
- 改用表格式布局编辑协议字段（修复行内控件挤压截断）
- 画布可视化数据源 → 协议配置关联曲线（品牌蓝 + 端点圆点）
- 支持显示组件**直接绑定协议字段**，运行器打开工程时自动合成隐式标签与数据绑定（无需手工建标签库）
- 修正帧头 + Length 拆帧：字段偏移相对负载，并按长度字段做边界校验
- 协议字段槽位按字段序号自动分配（移除手工标签地址配置）
- TLV 字段槽位上限随 `tagBytes` 自适应（1/2/3/4 字节）

---

## v0.2（2026-09-13）

- 数据源区分 TCP/UDP **客户端与服务端角色**（默认兼容旧工程）
- LogicPlanner 画布新增组件**对齐 / 等尺寸 / 等距分布**工具（图标工具条，锚点基准）
- 画布显示组件绑定协议字段的关联曲线（**青色**），与数据源 → 协议曲线区分
- 构建产物统一输出到 `out/build/<preset>` 根目录（exe 不再分散到子目录）
- 规约字段新增 **布尔 / 字符串 / 双精度 / 自定义枚举**类型（枚举取名称文本，字节宽度可配）

---

## v0.3（2026-09-14）

- 新增 **AI 配置规约**：经 LLM（OpenAI 兼容接口）按协议文档/自然语言描述生成拆帧参数与字段，预览后一键应用到协议配置组件
- 新增**协议字段一键生成显示组件**（按字段类型自动生成并绑定）
- 协议字段 **TLV 槽位默认继承前一条**；数值字段新增 **scale 工程换算**（工程值 = 原始值 × scale，带 scale 的隐式标签自动升为 Float32）；README 补充帧数据源说明

---

## v0.4（2026-09-14）

- 帧数据源新增**串口（COM）**传输方式（波特率/数据位/校验/停止位可配，与 TCP 走同一拆帧）
- 一键生成字段组件改为仅 `bool → 指示灯`，其余类型（整数/浮点/字符串/枚举）一律生成文本
- 数据源新增**「自动启动」开关**（默认关）：PageViewer 打开不主动连接，顶栏手动启动/停止数据源
- 新增 exe 应用图标（经 `.rc` 嵌入），移除冗余的报文调试面板与连接设置对话框
- 一键生成字段组件改为**纵向排列**并附带字段名文本；画布拖拽与多选交互优化（多选成员原地点击折叠为单选，拖动无位移不写 undo）
- 规约字段新增时**偏移默认接续上一字段末尾**（而非固定 0）
- 新增**「数据目的」组件**：按名称关联数据源，运行器把该数据源收到的原始帧原样转发到 TCP/UDP/串口端点（网关/上传场景）
- 新增**「协议测试数据」窗口**：按工程规约生成随机帧 HEX（TLV 逐字段 / 帧头+Length 全字段），可复制或保存 TXT 喂回链路验证
- 画布通信卡片显示**运行统计**：数据源卡「最后帧」时间、协议配置卡「帧计数」（成帧且命中至少一字段的帧数）

---

## v0.5（2026-09-14）

- 新增**「协议组」组件**：数据源可按组关联**多个协议合并解析**（一个数据源对接多条规约），并优化绑定卡片渲染与生成组件默认尺寸
- 协议组支持**多帧头**：不同帧头的协议共线传输，各协议字段只由**命中自身帧头**的帧驱动（帧头不命中的帧不污染其它协议字段）
- 绑定协议字段的**复合卡片**：抽取 `drawBindCard`，绑定文本组件改为复合卡片（同卡显「字段名 + 值」，不再单独生成字段名标签），并扩展到**指示灯 / 仪表 / 曲线**等显示组件
- 复合卡片**字段名字号支持统一调节**：在画布「页面属性」区设置，随工程保存

---

## v0.6（2026-09-14）

- 数据源传输方式新增**「监听」**：按固定三元组（监听 IP dip / 端口 dport / 协议）过滤，绑定端口即 dport（不另设本机端口）——UDP 绑定 dport、命中=源为 dip:dport（反向），dip=`*` 时全收（正向）；TCP 监听 dport、仅接受对端 IP=dip 的接入（连接内双向都解析）；未命中的报文/接入直接丢弃、不驱动字段
- 数据源监听新增**「镜像抓包」方式**（Npcap 混杂模式，交换机镜像/SPAN 场景）：监听「**别人之间**」的流量——固定三元组 dip/dport/协议，命中=**任一方向**（正向=目的 dip:dport、反向=源 dip:dport，收完整会话），自动放行 802.1Q VLAN 标签帧；属性面板下拉选择抓包网卡（NPF 设备名随工程持久化）
- Npcap 为**运行时可选依赖**：`wpcap.dll` 动态加载（自动探测 WinPcap 兼容模式与 `System32\Npcap` 安装位置），未安装时连接报安装提示、其余功能不受影响；**构建/测试不依赖 Npcap SDK**（回环端到端测试在无 Npcap 机器上自动跳过）
- 镜像 **TCP 逐流拆帧**：按四元组+方向为每条流各建拆帧器（上限 32 流），重传/乱序由拆帧器重同步兜底（尽力而为，不做序列号重组）；UDP 数据报天然成帧直接解析
- 监听默认「**本机端口**」方式（普通 socket 三元组过滤），旧工程行为不变（新属性自动回填默认值，无需升 schemaVersion）
- 项目品牌统一更名为 **PlanView**（命名空间 `pv`、静态库 `pv_base`、测试 `pv_tests`/PvTest 框架等），工程文件格式与两 exe 使用方式不变；README 补充 AI 配置规约说明并新增设计器/运行器/通信/AI 配置界面截图

---

## v0.7 — 性能版本：基准测试与转发/解析热路径优化（2026-09-15）

- 新增**性能基准测试**（`test_Perf`，仅 Release 构建运行、Debug 自动 `[skip]`）：**协议解析管线**（拆帧 → 结构解 → 字段解析，2 万帧样板全量对账）与**数据源→数据目的 UDP 回环转发**（2 万帧零丢失端到端对账：帧数/字节数/首帧内容/解析命中），输出吞吐参考值并以保守下限断言，数量级性能退化才判失败
- **修正转发设计缺陷**：数据目的转发原搭在 200 条上限的报文监视日志上，持续帧率超过「200 ÷ 轮询周期」即静默丢帧；现改走 `FrameDataSource` **专用转发队列**（`drainForwardFrames`，防御性上限 10 万条），监视日志裁剪不再波及转发，并补回归测试锁定（单次 pump 500 帧：转发队列全量、监视日志仍 200）
- **解析热路径优化**（协议解析 **14.8 万 → 24.8 万帧/秒**，+69%）：拆帧器改偏移消费（取帧只推进 head，免逐帧整体 memmove）；时间戳按 pump 批次计算（秒级显示，免逐帧 `localtime_s`）；解析值缓存改地址分组 vector 槽位（免逐字段 map 运算，后到者覆盖语义不变）；展示解析 hex 改查表 + 预留（免逐字节 `snprintf`）
- **UDP 收发热路径优化**（回环转发 **3.6 万 → 4.1 万帧/秒**，零丢失保持）：连接态 socket 发送走 `send()` 快路径（免逐包地址处理）；远端地址 `setRemote` 时解析一次缓存（免逐包 `inet_pton`）；内核收包缓冲加大至 **1MB**（默认 ~64KB 千余小包即溢出，设备突发先丢）；收包线程对端字符串缓存（同设备连发免逐包 `inet_ntop` + 组串）
- **Npcap 注入发送对比实验**：手工构 DLT_NULL/IPv4/UDP 包经 `pcap_sendpacket` 注入回环设备，可 100% 送达普通绑定的 UDP socket，但吞吐较 `sendto` 低约 12%（5.4 万 vs 6.0 万帧/秒）——**数据目的转发维持 socket 路径**；结论记入 README 防止重复验证
- README 重写为**痛点导向**（「现场痛点 → PlanView 解法」对照表 + 功能特征为主体，构建/架构后置），并记录各基准的吞吐参考值与测量口径
